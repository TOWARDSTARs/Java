重写：需要有继承关系，子类重写父类的方法
      1.方法名必须相同；方法体可以不同
      2.参数列表必须相同（区别于重载）
      3.修饰符：范围可以扩大但不能缩小   public > protected > default > private
      4.抛出的异常：范围可以被缩小，但不能扩大： Exception(大)--->ClassNotFoundException

为什么需要重写：
      1.父类的功能，子类不一定需要，或者不一定满足
       Alt+Insert：override 或  ctrl+o ；